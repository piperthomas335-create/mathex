import { Check, Clock3, FileImage, ShieldCheck, X } from "lucide-react"
import { WorkspaceShell } from "@/components/workspace-shell"
import { Badge } from "@/components/ui/badge"
import { Button } from "@/components/ui/button"
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card"

export default function AdminPage() { return <WorkspaceShell><div className="flex flex-col gap-6"><div><div className="flex items-center gap-2 text-sm font-medium text-primary"><ShieldCheck className="size-4" /> 管理员工作区</div><h1 className="mt-2 font-serif text-3xl font-semibold">待审核队列</h1><p className="mt-2 text-muted-foreground">AI 只起草，人来决定什么能进入公开知识库。</p></div><div className="grid gap-6 lg:grid-cols-[320px_1fr]"><Card><CardHeader><CardTitle className="text-base">提交列表</CardTitle><CardDescription>1 条待审核</CardDescription></CardHeader><CardContent><button className="w-full rounded-lg border bg-muted p-4 text-left"><div className="flex items-center justify-between"><Badge><Clock3 /> 待审核</Badge><span className="font-mono text-xs text-muted-foreground">刚刚</span></div><p className="mt-3 font-semibold">函数单调性讲义补充</p><p className="mt-1 text-xs text-muted-foreground">匿名同学 · 图片提交</p></button></CardContent></Card><Card><CardHeader><div className="flex items-center gap-2"><FileImage className="size-4" /><CardTitle>结构化草稿</CardTitle></div><CardDescription>AI 建议知识点：函数的单调性（91%）</CardDescription></CardHeader><CardContent className="flex flex-col gap-4">{[["01 题目","用定义证明函数在给定区间上的单调性。"],["02 错解","选取两个特殊点验证后直接下结论。"],["03 正解","任取区间内 x₁<x₂，通过 f(x₂)-f(x₁) 的符号完成证明。"],["04 反思","单调性证明中的任意性不可被特殊点代替。"]].map(([a,b]) => <div key={a} className="rounded-lg border p-4"><p className="text-xs font-medium text-primary">{a}</p><p className="mt-2 text-sm leading-relaxed">{b}</p></div>)}<div className="flex justify-end gap-2"><Button variant="outline"><X data-icon="inline-start" /> 退回</Button><Button><Check data-icon="inline-start" /> 审核并入库</Button></div></CardContent></Card></div></div></WorkspaceShell> }
